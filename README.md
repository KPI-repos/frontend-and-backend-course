# Plant Care Management System

## 📑 Зміст
- [Огляд](#огляд)
- [Тема проєкту](#тема-проєкту)
- [Особливості](#особливості)
- [Технологічний стек](#технологічний-стек)
- [Функціональні вимоги](#функціональні-вимоги)
- [Нефункціональні вимоги](#нефункціональні-вимоги)
- [Бізнес логіка](#бізнес-логіка)
- [Встановлення та налаштування](#встановлення-та-налаштування)
- [Структура проєкту](#структура-проєкту)
- [Ключові компоненти](#ключові-компоненти)
- [API Ендпоінти](#api-ендпоінти)
- [Моделі даних](#моделі-даних)
- [Оптимізація продуктивності](#оптимізація-продуктивності)
- [Розгортання](#розгортання)
- [Метрики продуктивності](#метрики-продуктивності)
- [Безпека](#безпека)
- [Усунення несправностей](#усунення-несправностей)
- [Внесок у проєкт](#внесок-у-проєкт)
- [Ліцензія](#ліцензія)
- [Висновки](#висновки)

## Огляд
Система управління рослинами - це веб-додаток, який дозволяє користувачам керувати інформацією про рослини, їх категорії та інструкції з догляду. Система включає функції аутентифікації користувачів та управління даними про рослини.

## Тема проєкту
Розробка системи управління рослинами з можливістю ведення каталогу рослин, їх категорій та інструкцій з догляду. Проєкт націлений на:
- Спрощення процесу відстеження та догляду за рослинами
- Створення централізованої бази даних рослин та інструкцій з догляду
- Надання зручного API для інтеграції з іншими системами 
- Забезпечення користувачів інструментами для ефективного управління колекціями рослин

## Особливості
- Аутентифікація та авторизація користувачів
- Управління каталогом рослин
- Класифікація рослин за категоріями
- Детальні інструкції з догляду за рослинами
- RESTful API для взаємодії з системою
- Пошук та фільтрація рослин за різними параметрами
- Завантаження та зберігання зображень рослин
- Статистика по догляду за рослинами

## Технологічний стек
- **Backend**: Node.js, Express.js
- **Мова програмування**: TypeScript
- **База даних**: PostgreSQL
- **ORM**: Prisma
- **Аутентифікація**: JWT, bcrypt
- **Валідація даних**: Zod
- **Тестування**: Jest, Supertest
- **Dependency Injection**: tsyringe
- **Документація API**: Swagger/OpenAPI
- **Логування**: Winston
- **Зберігання зображень**: Multer, AWS S3 (опціонально)
- **Розробка**: TypeScript, ts-node, nodemon

## Функціональні вимоги
1. Управління користувачами
   - Реєстрація користувачів
   - Аутентифікація користувачів
   - Управління профілем
   - Відновлення паролю
   - Різні ролі користувачів (адміністратор, звичайний користувач)

2. Управління рослинами
   - Додавання нових рослин
   - Редагування інформації про рослини
   - Видалення рослин
   - Перегляд списку рослин
   - Завантаження зображень рослин
   - Пошук рослин за назвою
   - Фільтрація рослин за категоріями

3. Управління категоріями
   - Створення категорій
   - Редагування категорій
   - Видалення категорій
   - Прив'язка рослин до категорій
   - Ієрархічна структура категорій

4. Управління інструкціями з догляду
   - Створення інструкцій
   - Редагування інструкцій
   - Видалення інструкцій
   - Прив'язка інструкцій до рослин
   - Шаблони інструкцій з догляду

5. Нагадування та календар
   - Створення нагадувань про полив та догляд
   - Перегляд календаря догляду
   - Сповіщення про необхідні дії

## Нефункціональні вимоги
1. **Продуктивність**
   - Швидкість відповіді API < 500ms
   - Обробка до 1000 запитів на хвилину
   - Оптимізація запитів до бази даних

2. **Безпека**
   - Захист від SQL-ін'єкцій
   - Безпечне зберігання паролів
   - Валідація вхідних даних
   - JWT аутентифікація
   - Захист від CSRF-атак
   - Обмеження швидкості запитів (rate limiting)

3. **Масштабованість**
   - Модульна архітектура
   - Підтримка горизонтального масштабування
   - Кешування часто використовуваних даних

4. **Надійність**
   - Обробка помилок
   - Логування подій
   - Резервне копіювання даних
   - Відмовостійкість системи

5. **Зручність використання**
   - Зрозумілі повідомлення про помилки
   - Документований API
   - Послідовний дизайн та структура ендпоінтів

## Бізнес логіка
1. Користувачі можуть створювати облікові записи та входити в систему
   - Кожен користувач має унікальний email та пароль
   - Паролі зберігаються в зашифрованому вигляді
   - Сесії користувачів реалізовані через JWT-токени

2. Користувачі можуть додавати, редагувати та видаляти рослини
   - Користувач може керувати лише власними рослинами
   - Адміністратори мають доступ до всіх рослин у системі
   - Кожна рослина має базові атрибути: назва, опис, зображення

3. Кожна рослина повинна належати до певної категорії
   - Категорії формують ієрархічну структуру
   - Одна рослина може належати до кількох категорій
   - Категорії допомагають організувати та фільтрувати рослини

4. Для кожної рослини можна створити інструкцію з догляду
   - Одна рослина може мати кілька інструкцій (для різних сезонів)
   - Інструкції з догляду містять часові інтервали виконання
   - Система може створювати нагадування на основі інструкцій

5. Інструкції з догляду містять інформацію про полив, освітлення та температуру
   - Частота поливу вказується в днях або тижнях
   - Вимоги до освітлення описуються в якісних термінах
   - Температурні режими вказуються мінімальними та максимальними значеннями

6. Система підтримує сезонні варіації в інструкціях з догляду
   - Різні режими поливу для літа та зими
   - Різні температурні режими для різних сезонів
   - Різні вимоги до освітлення протягом року

## Встановлення та налаштування
1. Клонуйте репозиторій:
```bash
git clone [repository-url]
```

2. Встановіть залежності:
```bash
npm install
```

3. Налаштуйте змінні середовища:
Створіть файл `.env` з наступними змінними:
```
DATABASE_URL="postgresql://user:password@localhost:5432/plant_care"
JWT_SECRET="your-secret-key"
JWT_EXPIRATION="1d"
PORT=3000
NODE_ENV="development"
```

4. Запустіть міграції бази даних:
```bash
npx prisma migrate dev
```

5. Запустіть сервер розробки:
```bash
npm run dev
```

6. Запуск тестів:
```bash
npm run test
```

7. Збірка для виробництва:
```bash
npm run build
```

## Структура проєкту
```
src/
├── auth/           # Аутентифікація та авторизація
│   ├── controllers/
│   ├── services/
│   ├── middlewares/
│   └── dto/
├── category/       # Управління категоріями
│   ├── controllers/
│   ├── services/
│   └── dto/
├── plant/          # Управління рослинами
│   ├── controllers/
│   ├── services/
│   └── dto/
├── care-guide/     # Управління інструкціями з догляду
│   ├── controllers/
│   ├── services/
│   └── dto/
├── user/           # Управління користувачами
│   ├── controllers/
│   ├── services/
│   └── dto/
├── common/         # Спільні компоненти
│   ├── middlewares/
│   ├── exceptions/
│   └── utils/
├── config/         # Конфігурація
├── db/             # Взаємодія з базою даних
├── di/             # Dependency Injection
├── logger/         # Логування
├── types/          # Типи TypeScript
├── app.ts          # Точка входу додатку
└── server.ts       # Налаштування сервера
```

## Ключові компоненти
1. **Моделі бази даних**
   - User: зберігає інформацію про користувачів
   - Plant: зберігає інформацію про рослини
   - Category: зберігає інформацію про категорії рослин
   - CareGuide: зберігає інструкції з догляду
   - PlantImage: зберігає посилання на зображення рослин
   - Reminder: зберігає нагадування про догляд

2. **Контролери**
   - AuthController: керує аутентифікацією та авторизацією
   - PlantController: керує операціями з рослинами
   - CategoryController: керує операціями з категоріями
   - CareGuideController: керує операціями з інструкціями
   - UserController: керує операціями з користувачами
   - ReminderController: керує нагадуваннями

3. **Сервіси**
   - AuthService: реалізує бізнес-логіку аутентифікації
   - PlantService: реалізує бізнес-логіку роботи з рослинами
   - CategoryService: реалізує бізнес-логіку роботи з категоріями
   - CareGuideService: реалізує бізнес-логіку роботи з інструкціями
   - UserService: реалізує бізнес-логіку роботи з користувачами
   - ReminderService: реалізує бізнес-логіку роботи з нагадуваннями

4. **Проміжне програмне забезпечення**
   - AuthMiddleware: перевіряє аутентифікацію та авторизацію
   - ErrorHandler: обробляє помилки та виняткові ситуації
   - ValidationMiddleware: валідує вхідні дані запитів
   - LoggingMiddleware: логує запити та відповіді
   - RateLimitMiddleware: обмежує кількість запитів

## API Ендпоінти

### Аутентифікація
- `POST /api/auth/register` - Реєстрація нового користувача
- `POST /api/auth/login` - Вхід в систему
- `POST /api/auth/refresh` - Оновлення токену доступу
- `POST /api/auth/logout` - Вихід із системи
- `POST /api/auth/forgot-password` - Запит на відновлення паролю

### Користувачі
- `GET /api/users/me` - Отримання інформації про поточного користувача
- `PATCH /api/users/me` - Оновлення профілю користувача
- `GET /api/users` - Отримання списку користувачів (тільки для адміністраторів)
- `GET /api/users/:id` - Отримання інформації про конкретного користувача
- `DELETE /api/users/:id` - Видалення користувача

### Рослини
- `GET /api/plants` - Отримання списку рослин
- `POST /api/plants` - Створення нової рослини
- `GET /api/plants/:id` - Отримання інформації про конкретну рослину
- `PATCH /api/plants/:id` - Оновлення інформації про рослину
- `DELETE /api/plants/:id` - Видалення рослини
- `POST /api/plants/:id/images` - Завантаження зображення для рослини

### Категорії
- `GET /api/categories` - Отримання списку категорій
- `POST /api/categories` - Створення нової категорії
- `GET /api/categories/:id` - Отримання інформації про конкретну категорію
- `PATCH /api/categories/:id` - Оновлення інформації про категорію
- `DELETE /api/categories/:id` - Видалення категорії
- `GET /api/categories/:id/plants` - Отримання рослин у конкретній категорії

### Інструкції з догляду
- `GET /api/care-guides` - Отримання списку інструкцій з догляду
- `POST /api/care-guides` - Створення нової інструкції
- `GET /api/care-guides/:id` - Отримання інформації про конкретну інструкцію
- `PATCH /api/care-guides/:id` - Оновлення інформації про інструкцію
- `DELETE /api/care-guides/:id` - Видалення інструкції
- `GET /api/plants/:id/care-guides` - Отримання інструкцій для конкретної рослини

### Нагадування
- `GET /api/reminders` - Отримання списку нагадувань
- `POST /api/reminders` - Створення нового нагадування
- `GET /api/reminders/:id` - Отримання інформації про конкретне нагадування
- `PATCH /api/reminders/:id` - Оновлення інформації про нагадування
- `DELETE /api/reminders/:id` - Видалення нагадування

## Моделі даних

### User
```typescript
model User {
  id          String   @id @default(uuid())
  email       String   @unique
  password    String
  name        String
  role        Role     @default(USER)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  plants      Plant[]
  careGuides  CareGuide[]
  reminders   Reminder[]
}

enum Role {
  USER
  ADMIN
}
```

### Plant
```typescript
model Plant {
  id          String      @id @default(uuid())
  name        String
  description String?
  scientificName String?
  origin      String?
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  userId      String
  user        User        @relation(fields: [userId], references: [id])
  categories  CategoryOnPlant[]
  careGuides  CareGuide[]
  images      PlantImage[]
  reminders   Reminder[]
}
```

### Category
```typescript
model Category {
  id          String      @id @default(uuid())
  name        String
  description String?
  parentId    String?
  parent      Category?   @relation("CategoryToCategory", fields: [parentId], references: [id])
  children    Category[]  @relation("CategoryToCategory")
  plants      CategoryOnPlant[]
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
}

model CategoryOnPlant {
  plantId     String
  categoryId  String
  plant       Plant     @relation(fields: [plantId], references: [id])
  category    Category  @relation(fields: [categoryId], references: [id])
  assignedAt  DateTime  @default(now())

  @@id([plantId, categoryId])
}
```

### CareGuide
```typescript
model CareGuide {
  id              String    @id @default(uuid())
  title           String
  wateringFrequency String
  lightRequirements String
  temperatureMin  Float?
  temperatureMax  Float?
  humidity        String?
  fertilizing     String?
  season          Season?
  additionalNotes String?
  plantId         String
  plant           Plant     @relation(fields: [plantId], references: [id])
  userId          String
  user            User      @relation(fields: [userId], references: [id])
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}

enum Season {
  SPRING
  SUMMER
  AUTUMN
  WINTER
  ALL_YEAR
}
```

## Оптимізація продуктивності
1. Кешування часто використовуваних запитів
   - Використання Redis для кешування
   - Стратегії інвалідації кешу
   - Кешування на рівні запитів

2. Оптимізація запитів до бази даних
   - Використання індексів
   - Оптимізація складних запитів
   - Пакетна обробка великих обсягів даних

3. Пагінація результатів
   - Обмеження кількості результатів на сторінку
   - Курсор-базована пагінація для великих наборів даних

4. Асинхронна обробка запитів
   - Використання черг для довготривалих операцій
   - Обробка фонових задач

5. Оптимізація зображень та медіафайлів
   - Стиснення зображень
   - Генерація мініатюр
   - Використання CDN для доставки статичних файлів

## Розгортання
1. Підготовка сервера
   - Встановлення Node.js та npm
   - Налаштування Nginx як зворотного проксі
   - Налаштування SSL-сертифікатів

2. Налаштування бази даних
   - Встановлення PostgreSQL
   - Створення бази даних та користувача
   - Налаштування резервного копіювання

3. Налаштування змінних середовища
   - Створення файлу `.env` з усіма потрібними змінними
   - Налаштування секретів для виробничого середовища

4. Запуск міграцій
   - Виконання міграцій бази даних
   - Перевірка цілісності даних

5. Запуск сервера
   - Використання PM2 для управління процесами
   - Налаштування автоматичного перезапуску
   - Моніторинг стану сервера

## Метрики продуктивності
1. Час відповіді API
   - Середній час відповіді < 500ms
   - 95-й перцентиль часу відповіді < 1000ms

2. Кількість запитів на хвилину
   - Підтримка до 1000 запитів на хвилину
   - Плавна деградація при перевантаженні

3. Використання CPU та пам'яті
   - Моніторинг використання ресурсів
   - Оптимізація споживання пам'яті

4. Кількість помилок
   - Моніторинг частоти помилок
   - Аналіз типів помилок

5. Час завантаження сторінок
   - Вимірювання часу завантаження API-відповідей
   - Оптимізація розміру відповідей

## Безпека
1. Захист від SQL-ін'єкцій через Prisma
   - Використання параметризованих запитів
   - Відсутність прямих SQL-запитів

2. Хешування паролів з використанням bcrypt
   - Надійне хешування паролів з сіллю
   - Налаштування факторів роботи

3. JWT для аутентифікації
   - Короткий термін дії токенів доступу
   - Використання токенів оновлення
   - Підписання токенів секретним ключем

4. Валідація вхідних даних через Zod
   - Валідація всіх вхідних даних
   - Типізація даних

5. Захист від CSRF-атак
   - Використання CSRF-токенів
   - Перевірка походження запитів

6. Обмеження кількості запитів
   - Обмеження кількості запитів з однієї IP-адреси
   - Обмеження кількості невдалих спроб входу

## Усунення несправностей
1. Перевірка логів сервера
   - Аналіз журналів помилок
   - Пошук причин проблем

2. Перевірка статусу бази даних
   - Моніторинг доступності бази даних
   - Перевірка навантаження на базу даних

3. Перевірка з'єднання з базою даних
   - Тестування підключення
   - Перевірка налаштувань з'єднання

4. Перевірка валідності JWT токенів
   - Налагодження проблем з автентифікацією
   - Перевірка терміну дії токенів

5. Перевірка правильності вхідних даних
   - Виявлення невалідних запитів
   - Аналіз помилок валідації

## Внесок у проєкт
Ми вітаємо внески до проєкту! Для того, щоб зробити внесок:

1. Форкніть репозиторій
2. Створіть гілку для ваших змін (`git checkout -b feature/amazing-feature`)
3. Внесіть зміни та закомітьте їх (`git commit -m 'Add some amazing feature'`)
4. Відправте гілку до вашого форку (`git push origin feature/amazing-feature`)
5. Відкрийте Pull Request

## Ліцензія
Цей проєкт ліцензовано за ліцензією MIT - див. файл [LICENSE](LICENSE) для деталей.

## Висновки
Система управління рослинами надає повний набір інструментів для ведення каталогу рослин та управління інформацією про них. Використання сучасного технологічного стеку забезпечує високу продуктивність, безпеку та масштабованість системи. Модульна архітектура дозволяє легко розширювати функціональність та підтримувати код.

Основні переваги системи:
- Типобезпечність завдяки TypeScript та Prisma
- Модульна архітектура з використанням принципів SOLID
- Захищений API з використанням JWT-автентифікації
- Гнучка система категоризації рослин
- Детальні інструкції з догляду за рослинами
- Зручний механізм нагадувань для регулярного догляду

Система може бути розширена додатковими функціями, такими як соціальні взаємодії між користувачами, обмін інформацією про рослини, інтеграція з системами розумного дому для автоматизованого догляду за рослинами.
